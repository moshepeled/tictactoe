export class Player {
    name: string;
    score: number;
    shape: string;
    constructor(name, score, shape) {
        this.name = name;
        this.score = score;
        this.shape = shape;
    }
}
